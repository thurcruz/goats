'use client'
import Link from 'next/link';import {usePathname} from 'next/navigation';import {useState} from 'react';import {AnimatePresence,motion} from 'framer-motion';import {Bot,BookOpen,CheckSquare,Home,Plus,Receipt,Target,UserRound,UsersRound,X} from 'lucide-react'
const nav=[{href:'/dashboard',label:'Hoje',icon:Home},{href:'/comunidade',label:'Comunidade',icon:UsersRound},{href:'#quick',label:'Adicionar',icon:Plus,action:true},{href:'/goat-ai',label:'Goat AI',icon:Bot},{href:'/perfil',label:'Perfil',icon:UserRound}];const actions=[{href:'/tarefas',label:'Tarefa ou hábito',icon:CheckSquare},{href:'/metas',label:'Meta',icon:Target},{href:'/financeiro',label:'Transação',icon:Receipt},{href:'/conhecimento',label:'Livro ou aprendizado',icon:BookOpen}]
export default function BottomNav(){const pathname=usePathname();const[open,setOpen]=useState(false);return <><AnimatePresence>{open&&<motion.div initial={{opacity:0}} animate={{opacity:1}} exit={{opacity:0}} className="fixed inset-0 z-50 bg-black/65 backdrop-blur-sm md:hidden" onClick={()=>setOpen(false)}><motion.div initial={{y:80,opacity:0}} animate={{y:0,opacity:1}} exit={{y:80,opacity:0}} className="glass absolute bottom-28 left-4 right-4 rounded-[28px] p-5" onClick={e=>e.stopPropagation()}><div className="mb-4 flex items-center justify-between"><div><p className="eyebrow">Ação rápida</p><h2 className="mt-1 text-xl font-semibold">O que vamos mover?</h2></div><button className="icon-button" onClick={()=>setOpen(false)}><X size={18}/></button></div><div className="grid grid-cols-2 gap-2">{actions.map(({href,label,icon:Icon})=><Link href={href} onClick={()=>setOpen(false)} key={href} className="surface flex min-h-24 flex-col justify-between p-4 text-sm text-white no-underline"><Icon size={19} className="text-[#d0e027]"/><span>{label}</span></Link>)}</div></motion.div></motion.div>}</AnimatePresence><nav className="glass fixed bottom-4 left-3 right-3 z-40 flex h-[74px] items-center justify-around rounded-[26px] px-2 md:hidden">{nav.map(({href,label,icon:Icon,action})=>action?<button key={label} aria-label={label} onClick={()=>setOpen(true)} className="-mt-7 grid h-14 w-14 place-items-center rounded-full border-[5px] border-[#090a08] bg-[#d0e027] text-[#11130f] shadow-[0_8px_30px_rgba(208,224,39,.25)]"><Icon size={23}/></button>:<Link key={href} href={href} className="flex w-[58px] flex-col items-center gap-1 text-[10px] no-underline" style={{color:pathname===href?'#d0e027':'#8d8f88'}}><Icon size={19}/><span>{label}</span></Link>)}</nav></>}

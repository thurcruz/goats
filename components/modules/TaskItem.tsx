'use client'
import type {Task} from '@/lib/types';import {Check,ExternalLink,Trash2} from 'lucide-react'
const labels={fixa:'Rotina',hoje:'Hoje',repasse:'Repassada'}
export default function TaskItem({task,onToggle,onDelete,showGoalLink}:{task:Task;onToggle:(task:Task)=>void;onDelete?:(id:string)=>void;showGoalLink?:boolean}){return <div className="group flex items-center gap-4 rounded-2xl border border-white/[.07] bg-white/[.025] p-4 transition hover:border-white/[.13] hover:bg-white/[.045]" style={{opacity:task.completed?.58:1}}><button aria-label={task.completed?'Marcar como pendente':'Concluir'} onClick={()=>onToggle(task)} className="grid h-7 w-7 shrink-0 place-items-center rounded-full border transition" style={{borderColor:task.completed?'var(--energy)':'rgba(255,255,255,.2)',background:task.completed?'var(--energy)':'transparent',color:'#11130f'}}>{task.completed&&<Check size={15}/>}</button><div className="min-w-0 flex-1"><p className={`truncate text-sm ${task.completed?'line-through text-white/40':'text-white'}`}>{task.title}</p>{showGoalLink&&task.goalId&&<span className="muted mt-1 flex items-center gap-1 text-[11px]"><ExternalLink size={10}/> Ligada a uma meta</span>}</div><span className="rounded-full border border-white/10 px-2.5 py-1 text-[10px] font-bold uppercase tracking-wider text-white/45">{labels[task.category]}</span>{onDelete&&<button aria-label="Excluir" onClick={()=>onDelete(task.id)} className="text-white/25 opacity-100 transition hover:text-danger md:opacity-0 md:group-hover:opacity-100"><Trash2 size={15}/></button>}</div>}
